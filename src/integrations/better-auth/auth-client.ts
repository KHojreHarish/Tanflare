import { createAuthClient } from 'better-auth/react'

export const authClient = createAuthClient({
  // Basic configuration - add more options as needed
  // The client will work with default settings
})
